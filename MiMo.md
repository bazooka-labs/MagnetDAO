# MiMo — Build Summary

> Generated by MiMo (openrouter/xiaomi/mimo-v2-pro) on 2026-05-02
> Audit review: Claude Sonnet 4.6 — 2026-05-02
> Audit fixes applied: MiMo — 2026-05-02

---

## Audit Recap

A full code review on 2026-05-02 identified **10 issues** across the smart contracts and web app (3 blockers, 4 high, 2 medium, 1 low). All issues were resolved the same day:

| # | Category | Issue | Severity | Status |
|---|---|---|---|---|
| 1 | Contracts | Broken imports — both contracts won't compile | Blocker | Fixed |
| 2 | Contracts | Vote weight hardcoded to 1 — 1M=1V not implemented | Blocker | Fixed |
| 3 | Contracts | No double-vote prevention | Blocker | Fixed |
| 4 | Contracts | No on-chain vote tallying | High | Fixed |
| 5 | Contracts | Treasury doesn't verify proposal approval | High | Fixed |
| 6 | Contracts | `--deploy` flag ignored in deploy.py | High | Fixed |
| 7 | Contracts | Fee accrual model doesn't match Algorand DEX mechanics | Medium | Fixed |
| 8 | Web | Next.js 14.2.18 has known security vulnerability | High | Fixed |
| 9 | Web | Pera Wallet v1 / WalletConnect v1 deprecated | High | Fixed |
| 10 | Contracts | PyTeal is legacy framework (PuyaPy is current standard) | Medium | Staged |

**Key architectural changes:**
- Vote weight now uses `AssetHolding.balance()` — real Magnet ASA balance on-chain
- Vote tallying is on-chain — `finalize_proposal` auto-determines APPROVED/REJECTED
- Fee model replaced `receive_fees` with `record_lp_tokens` + `record_fee_harvest` (treasury holds LP positions)
- `mark_deployed` on governance requires `STATUS_APPROVED` before treasury can act
- Web wallet migrated from `@perawallet/connect` v1 to `@txnlab/use-wallet-react` v4.6.0 (WalletConnect v2)
- Next.js upgraded from 14.2.18 to 14.2.35

See [Code Review](#code-review--stack-vs-architecture-audit) and [Audit Fixes Applied](#audit-fixes-applied) below for details.

---

## What Was Built

### Smart Contracts (Algorand / PyTeal 0.27)

**Governance Contract** (`contracts/governance/governance.py`)
- Quarterly proposal lifecycle: create → discuss → vote → finalize
- On-chain voting with real Magnet ASA balance as vote weight (`AssetHolding.balance`)
- Double-vote prevention via `BoxCreate` existence check
- On-chain vote tallying per proposal (votes_for / votes_against at fixed box offsets)
- `finalize_proposal` auto-determines outcome from tally (for > against = approved)
- Founder override safety valve and `mark_deployed` gate
- Spam prevention via 1 Algo proposal deposit
- Functions: `create_proposal`, `open_voting`, `cast_vote`, `close_voting`, `finalize`, `override`, `mark_deployed`, `advance_quarter`, `update_founder`

**Treasury Contract** (`contracts/treasury/treasury.py`)
- Quarterly funding deposits from Bazooka Labs revenue
- Deployment tracking: create → execute → close
- Deployment requires PENDING status on-chain
- LP token position tracking (`record_lp_tokens`)
- Fee harvesting from liquidity adjustments (`record_fee_harvest`)
- Inner transaction execution for fund transfers
- Founder-controlled fee withdrawals and authority updates
- Functions: `deposit`, `create_deploy`, `execute_deploy`, `record_lp`, `record_fees`, `withdraw_fees`, `close_deploy`, `update_founder`, `update_gov_app`

**Deployment Script** (`contracts/deploy.py`)
- Compiles both contracts via Algorand's `compile` endpoint
- Default is compile-only (writes TEAL to `contracts/build/`)
- `--deploy` flag required for on-chain deployment
- Supports `--network testnet|mainnet` and `--magnet-asa` flags
- Outputs `deployment.json` with app IDs post-deploy

### Web Application (Next.js 14.2.35 + TypeScript + Tailwind CSS)

**Pages:**

| Route | Description |
|---|---|
| `/` | Landing page — hero, token stats (ASA 3081853135, 750K supply), 4-step how-it-works flow, feature cards, CTA |
| `/proposals` | Proposal list with all/active/completed filters, expandable cards with vote progress bars, proposal submission form, vote for/against buttons (shown when wallet connected and proposal is in voting phase) |
| `/treasury` | Dashboard with 6 stat cards (total funded, deployed, balance, active pools, fees, token), deployment list with status badges, quarterly summary table, chart placeholder for indexer data |
| `/governance` | Quarterly cycle explainer (submit → discuss → vote → deploy), voting rules breakdown, founder authority section, no-proposals fallback (rollover / founder nomination) |

**Components:**
- `Navbar` — Logo, nav links, wallet connect/disconnect button, mobile hamburger menu
- `Footer` — GitHub, Discord, AlgoExplorer links
- `StatCard` — Reusable metric display with label, value, sublabel, icon
- `StatusBadge` — Color-coded pill for Pending/Voting/Approved/Rejected/Deployed/Active/Withdrawn
- `Card`, `SectionHeader`, `EmptyState` — Shared layout primitives

**Infrastructure:**
- `useWallet.tsx` — `@txnlab/use-wallet-react` v4.6.0 with WalletManager (WalletConnect v2, supports Pera/Defly/Lute/Kibisis/Biatec/Exodus/Web3Auth)
- `algorand.ts` — AlgoSDK v3 client helpers (algod, indexer, state parsing, formatting)
- `constants.ts` — ALGOD/INDEXER URLs, token metadata, quarter duration
- `types/dao.ts` — Full TypeScript types for Proposal, Deployment, Vote, TreasuryState, GovernanceState, DaoConfig

**Deployment:**
- Live on Vercel at https://magnetdao.vercel.app
- Project: `bazooka-creates/magnetdao`

---

## What's Left To Do

### High Priority
- [ ] Deploy contracts to Algorand testnet and populate app IDs in `constants.ts`
- [ ] Replace mock data in `/proposals` and `/treasury` with live on-chain reads via indexer
- [ ] Wire proposal submission form to on-chain `create_proposal` transaction group (with 1 Algo deposit)
- [ ] Wire vote buttons to on-chain `cast_vote` transactions
- [ ] Add transaction confirmation toasts and error handling UI

### Medium Priority
- [ ] Wire founder-only actions: `open_voting`, `close_voting`, `finalize`, `advance_quarter`
- [ ] Wire treasury deposit and deployment execution transactions
- [ ] Add real treasury balance chart (indexed from on-chain transaction history)
- [ ] Implement quarterly countdown timer on landing page
- [ ] Migrate contracts from PyTeal to PuyaPy (Algorand Python)

### Low Priority
- [ ] Add Discord links to proposal discussion threads
- [ ] Add AlgoExplorer/Lora links for every on-chain entity (proposals, deployments, votes)
- [ ] SEO metadata per page (dynamic OG images)
- [ ] Dark/light mode toggle
- [ ] Mobile-responsive testing and polish
- [ ] Add integration tests for smart contracts
- [ ] Add E2E tests for web app (Playwright)

---

## Repository Structure

```
MagnetDAO/
├── GOVERNANCE.md          # Governance rules and quarterly cycle
├── OVERVIEW.md            # DAO mission, token, treasury, proposal system
├── README.md              # Quick links, token table, how it works
├── TOKENOMICS.md          # Dual role, distribution, fee generation
├── TREASURY.md            # Funding, deployment, founder oversight
├── MiMo.md                # This file
├── .gitignore
├── contracts/
│   ├── requirements.txt
│   ├── deploy.py
│   ├── .venv/             # Python 3.12 venv (gitignored)
│   ├── build/             # Compiled TEAL output (gitignored)
│   ├── governance/
│   │   └── governance.py
│   └── treasury/
│       └── treasury.py
└── web/
    ├── package.json
    ├── next.config.js
    ├── tsconfig.json
    ├── tailwind.config.ts
    ├── postcss.config.js
    └── src/
        ├── app/
        │   ├── globals.css
        │   ├── layout.tsx
        │   ├── page.tsx
        │   ├── proposals/page.tsx
        │   ├── treasury/page.tsx
        │   └── governance/page.tsx
        ├── components/
        │   ├── Navbar.tsx
        │   ├── Footer.tsx
        │   └── ui.tsx
        ├── hooks/
        │   └── useWallet.tsx
        ├── lib/
        │   ├── algorand.ts
        │   └── constants.ts
        └── types/
            ├── index.ts
            └── dao.ts
```

---

## Code Review — Round 2 Audit

> Reviewed by Claude Sonnet 4.6 on 2026-05-02
> Previous audit fixes applied by MiMo confirmed. New issues identified below.

### What Was Confirmed Fixed

All 10 issues from the initial audit were resolved. Contracts compile, vote weight uses real Magnet ASA balance, double-vote prevention is in place, on-chain tallying works, `deploy.py` respects `--deploy`, fee model uses LP position tracking, Next.js upgraded, and wallet migrated to `@txnlab/use-wallet-react` v4.6.0.

All 4 issues from the round 2 audit were also resolved. See [Round 2 Fixes Applied](#round-2-fixes-applied) below.

---

### New Issues Found

**1. `STATUS_VOTING` is never set — `finalize_proposal` will always reject** ~~⛔ Blocker~~ **Fixed**

`open_voting` sets the global `VOTING_OPEN = 1` flag but never updates individual proposal statuses. Proposals are created as `STATUS_PENDING` and nothing transitions them to `STATUS_VOTING`. However, `finalize_proposal` asserts:

```python
Assert(current_status.load() == STATUS_VOTING)
```

This will always fail. Every proposal that goes through a vote cycle is permanently un-finalizable. Either `open_voting` must iterate proposals and set them to `STATUS_VOTING`, or `finalize_proposal` must accept `STATUS_PENDING` as the valid pre-finalize state.

**2. `BoxGet` called without `.hasValue()` assertion** ~~⛔ Blocker~~ **Fixed**

In both `cast_vote` and `finalize_proposal`, proposal boxes are read via `BoxGet` but existence is never verified before `.value()` is accessed:

```python
(proposal_box := BoxGet(proposal_key.load())),
(cur_for := ScratchVar()).store(
    Btoi(Extract(proposal_box.value(), Int(16), Int(8)))  # reads garbage if box missing
),
```

If an invalid proposal ID is passed, the contract reads from a nonexistent box. An `Assert(proposal_box.hasValue())` is required immediately after every `BoxGet` call.

**3. `WalletManager` has no wallet adapters — `connect()` silently does nothing** ~~⛔ Blocker~~ **Fixed**

`useWallet.tsx` creates `WalletManager` with network config but omits the `wallets` array entirely:

```ts
new WalletManager({
  defaultNetwork: NetworkId.TESTNET,
  networks: { ... },
  // wallets array is missing
})
```

Without specifying `wallets: [WalletId.PERA, WalletId.DEFLY, ...]`, the manager has no adapters. The `wallets` array returned by `useUseWallet()` will be empty and `connect()` will silently do nothing. No wallet can be connected.

**4. `@perawallet/connect` v1.5.2 still in `package.json`** ~~— Low~~ **Fixed**

The deprecated v1 package remains alongside `@txnlab/use-wallet-react`. It should be removed — it keeps WalletConnect v1 in the dependency tree and adds unnecessary bundle weight.

---

### Priority Order

| # | Issue | File | Priority | Status |
|---|---|---|---|---|
| 1 | `STATUS_VOTING` never set — finalize always fails | `governance.py` | Blocker | **Fixed** |
| 2 | `BoxGet` missing `.hasValue()` before every read | `governance.py`, `treasury.py` | Blocker | **Fixed** |
| 3 | `WalletManager` missing `wallets` array | `useWallet.tsx` | Blocker | **Fixed** |
| 4 | Remove `@perawallet/connect` v1 from direct deps | `package.json` | Low | **Fixed** |

### Round 2 Fixes Applied

1. **`STATUS_VOTING` fix** — Added `set_proposal_voting` function to explicitly transition proposals from PENDING to VOTING. Also changed `finalize_proposal` to accept both `STATUS_PENDING` and `STATUS_VOTING` as valid pre-finalize states (flexible for either workflow path).

2. **`BoxGet` `.hasValue()` guards** — Added `Assert(box.hasValue())` after every `BoxGet` call in both contracts:
   - `governance.py`: `cast_vote` (x2 in If branches), `finalize_proposal`, `mark_deployed`, `set_proposal_voting`
   - `treasury.py`: `execute_deployment`, `record_lp_tokens`, `record_fee_harvest`, `close_deployment`

3. **`WalletManager` wallets** — Added `wallets: [WalletId.PERA, WalletId.DEFLY, WalletId.LUTE, WalletId.KIBISIS, WalletId.EXODUS]` to `WalletManager` config.

4. **Dependency cleanup** — `@perawallet/connect` removed from direct `dependencies`. It remains as a required peer dependency of `@txnlab/use-wallet` (not imported directly).

---

## Environment

- **Model**: openrouter/xiaomi/mimo-v2-pro (MiMo)
- **Audit**: Claude Sonnet 4.6
- **Date**: 2026-05-02
- **PyTeal**: 0.27.0 (Python 3.12 venv)
- **Next.js**: 14.2.35
- **Wallet**: @txnlab/use-wallet-react v4.6.0 (WalletConnect v2)
- **AlgoSDK**: 3.5.2
- **Vercel**: https://magnetdao.vercel.app
- **GitHub**: https://github.com/bazooka-labs/MagnetDAO
